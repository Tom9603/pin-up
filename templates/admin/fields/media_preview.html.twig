{% for media in field.value %}
    <img src="{{ asset('media/' ~ media.fileName) }}"
         alt="{{ media.fileName }}"
         style="max-width: 120px; border-radius: 8px; margin: 5px; box-shadow: 0 2px 6px rgba(0,0,0,0.3);">
{% else %}
    <em>Aucune image</em>
{% endfor %}
