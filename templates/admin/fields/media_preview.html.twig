{% if field.value %}
    <img src="{{ asset('media/' ~ field.value) }}"
         alt="aperçu"
         style="max-width: 100px; border-radius: 8px; margin-top: 10px;">
{% else %}
    <em>Aucune image</em>
{% endif %}
