{% extends 'base.html.twig' %}

{% block title %}Boutique{% endblock %}

{% block body %}

    <section class="shop-scope">

        <h1 class="shop-title">Boutique</h1>

        {{ include('shop/_cart_summary.html.twig') }}

        {% if products is empty %}
            <p class="shop-empty">Aucun produit disponible pour le moment.</p>
        {% else %}
            <div class="shop-grid">
                {% for product in products %}
                    <article class="shop-card">

                        {% if product.imageName %}
                            <img src="{{ asset('uploads/images/' ~ product.imageName) }}"
                                 alt="{{ product.name }}"
                                 class="shop-card__img">
                        {% endif %}

                        <div class="shop-card__title">{{ product.name }}</div>

                        {% if product.description %}
                            <div class="shop-card__desc">
                                {{ product.description|sanitize_html }}
                            </div>
                        {% endif %}

                        <div class="shop-card__price">
                            {{ (product.price / 100)|number_format(2, '.', ' ') }} €
                        </div>

                        {% if product.stock is not null %}
                            {% if product.stock == 0 %}
                                <span class="shop-card__stock shop-card__stock--out">Rupture de stock</span>
                            {% elseif product.stock <= 5 %}
                                <span class="shop-card__stock shop-card__stock--low">Plus que {{ product.stock }} en stock</span>
                            {% endif %}
                        {% endif %}

                        <a class="shop-btn shop-btn--primary"
                           href="{{ path('app_shop_product', { id: product.id }) }}">
                            Voir le produit
                        </a>
                    </article>
                {% endfor %}

            </div>
        {% endif %}

    </section>

{% endblock %}
