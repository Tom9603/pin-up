{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert" aria-live="assertive">{{ flash_error }}</div>
    {% endfor %}

    <h1>Inscription</h1>

    <section class="register-container" role="main">
        {{ form_start(registrationForm) }}

        <div class="form-group">
            {{ form_label(registrationForm.name) }}
            {{ form_widget(registrationForm.name) }}
            {% for error in registrationForm.name.vars.errors %}
                <p class="form-error name-error">{{ error.message }}</p>
            {% endfor %}
        </div>

        <div class="form-group">
            {{ form_label(registrationForm.email) }}
            {{ form_widget(registrationForm.email) }}
            {% for error in registrationForm.email.vars.errors %}
                <p class="form-error email-error">{{ error.message }}</p>
            {% endfor %}
        </div>

        <div class="password-wrapper">
            {{ form_label(registrationForm.plainPassword) }}
            <div class="password-field">
                {{ form_widget(registrationForm.plainPassword) }}
                <i class="fa-solid fa-eye toggle-password"
                   onclick="togglePassword(this)"
                   aria-label="Afficher ou masquer le mot de passe"></i>
            </div>
            {% for error in registrationForm.plainPassword.vars.errors %}
                <p class="form-error password-error">{{ error.message }}</p>
            {% endfor %}
        </div>

        <p>Votre mot de passe doit contenir au moins 8 caractères, une majuscule, une minuscule et un chiffre.</p>

        <div class="form-check">
            {{ form_widget(registrationForm.agreeTerms) }}
            {{ form_label(registrationForm.agreeTerms) }}
            {% for error in registrationForm.agreeTerms.vars.errors %}
                <p class="form-error terms-error">{{ error.message }}</p>
            {% endfor %}
        </div>

        <button type="submit" class="btn">S'inscrire</button>
        {{ form_end(registrationForm) }}

        <div class="log">
            <a href="{{ path('app_login') }}">Déjà un compte ? Connectez-vous</a>
        </div>
    </section>

    <script>
        function togglePassword(icon) {
            const input = icon.previousElementSibling;
            if (input.type === "password") {
                input.type = "text";
                icon.classList.replace("fa-eye", "fa-eye-slash");
            } else {
                input.type = "password";
                icon.classList.replace("fa-eye-slash", "fa-eye");
            }
        }
    </script>
{% endblock %}
