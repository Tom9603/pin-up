<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}Miss Pin-Up Bretagne{% endblock %}</title>

    <link rel="icon" type="image/png" href="{{ asset('images/favicon.png') }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Limelight&family=Lobster&display=swap" rel="stylesheet">

    {# FontAwesome CDN — simple, immédiat, fonctionne avec AssetMapper #}
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
          crossorigin="anonymous"
          referrerpolicy="no-referrer"
    />

    {% block importmap %}
        {{ importmap('app') }}
    {% endblock %}

    {% block stylesheets %}

    {% endblock %}
</head>

<body class="{{ body_class|default('') }}">

{% if body_class|default('') != 'login-page' %}
    {% include 'partials/_header.html.twig' %}
{% endif %}

<div class="stars"></div>

<main>
    {% block body %}{% endblock %}
</main>

{% include 'partials/_footer.html.twig' %}

{% block javascripts %}{% endblock %}

</body>
</html>
